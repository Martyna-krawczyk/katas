using System;
using TicTacToe;
using Xunit;

namespace TicTacToeTests
{
    public class AppRunnerTests
    {
        // [Fact]
        // public void InvalidCoordinatesReturnException()
        // {
        //     var input = new ConsoleInput();
        //     var output = new ConsoleOutput();
        //     var runner = new AppRunner(input, output);
        //     runner.Run();
        //
        //     var exception = Assert.Throws<ArgumentException>(() => runner.Run());
        //
        //     Assert.Equal("Sorry - that format is incorrect. Enter x ,y coordinates between 1-3 or 'q' to quit:", exception.Message);
        // }
    }
}